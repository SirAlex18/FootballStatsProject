# Database Configuration
DB_PASSWORD=your_secure_password_here

# External API Configuration
RAPIDAPI_KEY=your_actual_rapidapi_key_here

# Optional: Override default connection string if needed locally
# ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=footballstats;Username=postgres;Password=${DB_PASSWORD}
